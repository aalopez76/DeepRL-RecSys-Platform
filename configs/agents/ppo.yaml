# PPO Agent Hyperparameters
name: "ppo"
hyperparams:
  learning_rate: 0.0003
  gamma: 0.99
  gae_lambda: 0.95
  clip_ratio: 0.2
  epochs_per_update: 10
  batch_size: 64
  entropy_coeff: 0.01
  value_coeff: 0.5
  hidden_dims: [128, 64]
